# Description: Network bandwidth measure tool, version 3
# URL:         https://github.com/esnet/iperf
# Depends on:  openssl zlib

name=iperf3
version=3.19
release=1
source=$name-$version.tar.gz::https://github.com/esnet/iperf/archive/$version.tar.gz

build() {
	mkdir build; cd build

	export CFLAGS="$CFLAGS -flto=auto"

	../iperf-$version/configure  \
		--prefix=/usr        \
		--sysconfdir=/etc    \
		--disable-static     \

	make V=1
	make DESTDIR=$PKG install

	# remove junk
	rm $PKG/usr/lib/libiperf.la
}
