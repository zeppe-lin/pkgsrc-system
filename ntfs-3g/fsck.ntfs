#!/bin/sh
# A wrapper script that discards any unsupported flags from /etc/rc.
# Thanks to John McQuah, jmcquah at disroot dot org.

ARGS=$(echo "$*" | sed 's/ -a/ -d/; s/ -p/ -d/; s/ -f/ /;')

# Intentional.
# shellcheck disable=2086
exec /usr/bin/ntfsfix $ARGS

# End of file.
