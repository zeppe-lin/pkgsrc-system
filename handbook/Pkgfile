# Description: Zeppe-Lin handbook
# URL:         https://github.com/zeppe-lin/handbook

name=handbook
version=1.0.1-bb0a063
release=1
source=https://github.com/zeppe-lin/$name/archive/${version#*-}/$name-$version.tar.gz

build() {
	cd $name-${version#*-}*

	make DESTDIR=$PKG install-man
}
